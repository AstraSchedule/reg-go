# 复制为 .env 后填写。Vite 只会把 VITE_ 前缀的变量注入前端。
#
# 注意：注册站仅依赖构建模式区分开发/生产（import.meta.env.DEV），
# 生产构建请务必显式设置下面两项，否则请求会打到同源地址。
VITE_API_BASE=https://to.getastra.cn
VITE_ASTRA_API_BASE=https://class.getastra.cn
#
# ESA AI 验证码：身份标取自 ESA 控制台「AI 验证码 → 配置」页右上角，
# 场景 ID 取自对应规则条目的「场景ID」列。
# 身份标由 Vite 替换进 index.html 的 window.AliyunCaptchaConfig（%VITE_CAPTCHA_PREFIX%），
# 必须填真实值，否则验证码不会初始化、注册按钮会一直处于禁用状态。
VITE_CAPTCHA_PREFIX=your_esa_captcha_identity
VITE_CAPTCHA_SCENE_ID=your_esa_captcha_scene_id
